<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springboot.ckb.common.security.repository.OauthMemRepository">

    <!-- ✅ provider + providerId로 사용자 조회 -->
    <select id="findByProviderAndProviderId" parameterType="map" resultType="com.springboot.ckb.common.security.domain.OauthMember">
        SELECT
            id,
            email,
            name,
            provider,
            provider_id AS providerId,
            created_at AS createdAt,
            updated_at AS updatedAt
        FROM oauth_member
        WHERE provider = #{provider}
          AND provider_id = #{providerId}
    </select>

    <!-- ✅ 이메일로 사용자 조회 -->
    <select id="findByEmail" parameterType="string" resultType="com.springboot.ckb.common.security.domain.OauthMember">
        SELECT
            id,
            email,
            name,
            provider,
            provider_id AS providerId,
            created_at AS createdAt,
            updated_at AS updatedAt
        FROM oauth_member
        WHERE email = #{email}
    </select>

    <insert id="insertOauthMember" parameterType="com.springboot.ckb.common.security.domain.OauthMember" keyProperty="id">
	  <selectKey keyProperty="id" resultType="long" order="BEFORE">
	    SELECT oauth_member_seq.NEXTVAL FROM dual
	  </selectKey>
	  INSERT INTO oauth_member (
	    id,
	    email,
	    name,
	    provider,
	    provider_id,
	    created_at,
	    updated_at
	  ) VALUES (
	    #{id},
	    #{email},
	    #{name},
	    #{provider},
	    #{providerId},
	    sysdate,
	    sysdate
	  )
  </insert>


    <!-- ✅ 기존 Oauth 사용자 정보 업데이트 -->
    <update id="updateOauthMember" parameterType="com.springboot.ckb.common.security.domain.OauthMember">
        UPDATE oauth_member
        SET
            name = #{name},
            email = #{email},
            updated_at = NOW()
        WHERE provider = #{provider}
          AND provider_id = #{providerId}
    </update>

    <!-- ✅ 사용자 삭제 -->
    <delete id="deleteById" parameterType="long">
        DELETE FROM oauth_member WHERE id = #{id}
    </delete>

</mapper>
