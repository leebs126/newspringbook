<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springboot.ckb.common.security.repository.RefreshTokenRepository">

    <!-- 시퀀스를 이용한 INSERT -->
    <insert id="insertRefreshToken" parameterType="refreshToken"  keyProperty="id">
        <selectKey keyProperty="id" resultType="long" order="BEFORE">
	    SELECT token_seq.NEXTVAL FROM dual
	  </selectKey>
        INSERT INTO REFRESH_TOKEN 
        (id, 
        user_id, 
        refresh_token )
         VALUES (
        #{id}, 
        #{userId}, 
        #{refreshToken})
    </insert>

    <!-- userId로 조회 -->
    <select id="findByUserId" parameterType="String"
            resultType="refreshToken">
        SELECT id, user_id AS userId, refresh_token AS refreshToken
        FROM REFRESH_TOKEN
        WHERE user_id = #{userId}
    </select>

    <!-- 토큰 업데이트 -->
    <update id="updateRefreshToken">
        UPDATE REFRESH_TOKEN
        SET refresh_token = #{refreshToken}
        WHERE user_id = #{userId}
    </update>

    <!-- 삭제 -->
    <delete id="deleteByUserId" parameterType="long">
        DELETE FROM REFRESH_TOKEN
        WHERE user_id = #{userId}
    </delete>

</mapper>
