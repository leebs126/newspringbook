<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springboot.ckb.member.repository.MemberRepository">
	<resultMap id="memResult" type="member">
		<result property="memId" column="memId" />
        <result property="pwd" column="pwd" />
        <result property="name" column="name" />
        <result property="email" column="email" />
        <result property="joinDate" column="joinDate" />
	</resultMap> 

	<select id="selectAllMemberList" resultMap="memResult">
      <![CDATA[
         SELECT * FROM t_member3	
         ORDER BY joinDate DESC	 	
      ]]>
	</select>
    
  <insert id="insertMember"  parameterType="member">
	<![CDATA[
		 INSERT INTO t_member3(memId,
		                       pwd, 
		                       name,
		                       nickname, 
		                       email)
		 				VALUES(#{memId}, 
		 				       #{pwd}, 
		 				       #{name},
		 				       #{nickname}, 
		 				       #{email})
	]]>      
	</insert>
	
   
   <update id="updateMember"  parameterType="member">
     <![CDATA[
	     UPDATE t_member3
	     SET pwd=#{pwd}, 
	         name=#{name},
	         nickname=#{nickname}, 
	         email=#{email}
	     WHERE
	     memId=#{memId}
      ]]>      
   </update> 
     
   <delete id="deleteAccount"  parameterType="String">
	<![CDATA[
	   DELETE FROM  t_member3
	   WHERE
	   memId=#{memId}
	]]>      
  </delete>
  <select id="selectrMemberById"  resultType="member"   parameterType="String" >
	<![CDATA[
		SELECT memId,
		       pwd,
		       name,
		       email,
		       nickname,
		       role,
		       joindate
		FROM t_member3	
		WHERE memId=#{memId} 
	]]>
  </select>
</mapper>